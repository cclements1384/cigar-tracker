@page "/"

<PageTitle>Home</PageTitle>

<div class="hero-section">
    <img src="images/cigar-hero.jpg" alt="Cigar" class="hero-image" />
    <div class="hero-overlay">
        <h1>🔥 Cigar Tracker</h1>
        <p>Track, rate, and remember your favorite cigars</p>
        <p class="welcome-text">Welcome to your new app.</p>
    </div>
</div>

<style>
    .hero-section {
        position: relative;
        width: 100%;
        height: 400px;
        overflow: hidden;
        border-radius: 12px;
        margin-bottom: 32px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .hero-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, rgba(0, 0, 0, 0.4) 0%, rgba(0, 0, 0, 0.6) 100%);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        color: white;
        text-align: center;
    }

    .hero-overlay h1 {
        font-size: 3rem;
        font-weight: 600;
        margin: 0 0 12px 0;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    .hero-overlay p {
        font-size: 1.25rem;
        margin: 8px 0;
        text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
    }

    .welcome-text {
        font-size: 1rem;
        margin-top: 16px;
        opacity: 0.9;
    }
</style>
